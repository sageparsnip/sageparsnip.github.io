document.addEventListener("DOMContentLoaded", () => { //JS On-Ready! Fire start-up commands here.
	resetPage()
});

function resetPage(){
	createActorPane();
	createTrackerPane();
	createDetailsPane();
}
